import { CopyiconDirective } from './copyicon.directive';

describe('CopyiconDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyiconDirective();
    expect(directive).toBeTruthy();
  });
});
