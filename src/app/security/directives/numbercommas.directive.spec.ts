import { NumbercommasDirective } from './numbercommas.directive';

describe('NumbercommasDirective', () => {
  it('should create an instance', () => {
    const directive = new NumbercommasDirective();
    expect(directive).toBeTruthy();
  });
});
